= form_for current_user, url: settings_path do |form|
  = render 'shared/addresses',  address_obj: obj, ff: form
  .text-center.general-text-align
    = form.submit :save, class: 'btn btn-default', value: 'Save'